---
phase: quick-260415-u8k
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [run-pydocket-all-python-versions]
must_haves:
  truths:
    - "pydocket tests run against Python 3.10, 3.11, 3.12, 3.13, and 3.14"
    - "Each version uses a fresh venv with burner-redis installed from local build"
    - "Test results per version are reported with pass/fail/skip counts"
  artifacts: []
  key_links: []
---

<objective>
Run the pydocket test suite against all Python versions pydocket supports (3.10, 3.11, 3.12, 3.13, 3.14) using burner-redis as the in-memory backend.

Purpose: Verify burner-redis compatibility across every supported Python version before release.
Output: Console output with pass/fail/skip results per Python version. No files modified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

Key facts:
- pydocket source: /Users/alexander/dev/chrisguidry/docket
- pydocket requires-python: >=3.10 (classifiers: 3.10, 3.11, 3.12, 3.13, 3.14)
- burner-redis uses abi3-py310 (single .so works for all Python >= 3.10)
- pydocket branch to use: replace-fakeredis-with-burner-redis
- REDIS_VERSION=memory selects in-memory (burner-redis) backend
- conftest.py skips pub/sub tests on Python < 3.12 with memory backend (skip_memory_pubsub)
- CI test command: REDIS_VERSION=memory pytest tests/ -x --timeout=30 --numprocesses=logical --maxprocesses=4 --import-mode=importlib -o "addopts="
- uv is available at /opt/homebrew/bin/uv for Python version management
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build burner-redis wheel from source</name>
  <files></files>
  <action>
Build burner-redis as a wheel that can be installed into multiple venvs. Run from the burner-redis repo root (/Users/alexander/dev/prefectlabs/burner-redis):

```bash
maturin build --release
```

This produces an abi3 wheel in target/wheels/ that works with any Python >= 3.10. Note the wheel path for use in the next task.
  </action>
  <verify>
    <automated>ls /Users/alexander/dev/prefectlabs/burner-redis/target/wheels/*.whl</automated>
  </verify>
  <done>A .whl file exists in target/wheels/ containing the abi3 build of burner-redis</done>
</task>

<task type="auto">
  <name>Task 2: Run pydocket tests against each Python version</name>
  <files></files>
  <action>
For each Python version in [3.10, 3.11, 3.12, 3.13, 3.14], do the following:

1. Create a temporary venv using uv:
   ```bash
   uv venv /tmp/pydocket-test-3.XX --python 3.XX
   ```

2. Install the burner-redis wheel from Task 1:
   ```bash
   uv pip install --python /tmp/pydocket-test-3.XX /path/to/burner_redis-*.whl
   ```

3. Install pydocket and its test dependencies from the local checkout at /Users/alexander/dev/chrisguidry/docket (use the replace-fakeredis-with-burner-redis branch -- check it out first if not already on that branch):
   ```bash
   uv pip install --python /tmp/pydocket-test-3.XX -e "/Users/alexander/dev/chrisguidry/docket[metrics]"
   uv pip install --python /tmp/pydocket-test-3.XX --group dev --directory /Users/alexander/dev/chrisguidry/docket
   ```

4. Run the pydocket tests with REDIS_VERSION=memory:
   ```bash
   REDIS_VERSION=memory /tmp/pydocket-test-3.XX/bin/python -m pytest /Users/alexander/dev/chrisguidry/docket/tests/ -x --timeout=30 --numprocesses=logical --maxprocesses=4 --import-mode=importlib -o "addopts="
   ```

5. Record the exit code and summary line (passed/failed/skipped/errors) for each version.

6. Clean up the temp venv after each run:
   ```bash
   rm -rf /tmp/pydocket-test-3.XX
   ```

After all versions complete, present a summary table:

| Python | Result | Passed | Failed | Skipped | Errors | Notes |
|--------|--------|--------|--------|---------|--------|-------|

Note: Python 3.10 and 3.11 will skip some pub/sub tests (skip_memory_pubsub marker). This is expected.

If any version fails to install dependencies (e.g., a package lacks a wheel for 3.14), note it and continue with the remaining versions. The goal is maximum coverage, not all-or-nothing.

IMPORTANT: Use a generous timeout (300s) for each pytest run -- the full pydocket suite with xdist workers may take a while.
  </action>
  <verify>
    <automated>echo "Manual verification: review the summary table output from this task"</automated>
  </verify>
  <done>All 5 Python versions (3.10-3.14) have been tested or have documented reasons for being skipped, with pass/fail/skip counts reported per version</done>
</task>

</tasks>

<verification>
- burner-redis wheel builds successfully
- pydocket tests execute on each Python version 3.10-3.14
- Results summary table produced with pass/fail/skip counts
</verification>

<success_criteria>
- pydocket test suite runs on all 5 supported Python versions (3.10, 3.11, 3.12, 3.13, 3.14)
- Results are clearly reported per version
- Any failures or version-specific issues are documented
</success_criteria>

<output>
After completion, create `.planning/quick/260415-u8k-run-the-pydocket-tests-against-all-suppo/260415-u8k-SUMMARY.md`
</output>
