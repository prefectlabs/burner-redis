---
phase: quick
plan: 260415-us1
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - .github/workflows/pydocket-compat.yml
autonomous: true
must_haves:
  truths:
    - "CI test job runs against Python 3.10, 3.11, 3.12, 3.13, and 3.14"
    - "pydocket-compat job runs against Python 3.10, 3.11, 3.12, 3.13, and 3.14"
    - "Integration test job remains on Python 3.12 only (not matrixed)"
    - "All Python versions run even if one fails (fail-fast: false)"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Matrix strategy on test job"
      contains: "python-version"
    - path: ".github/workflows/pydocket-compat.yml"
      provides: "Matrix strategy on pydocket-compat job"
      contains: "python-version"
  key_links: []
---

<objective>
Add Python version matrix (3.10, 3.11, 3.12, 3.13, 3.14) to the unit test and pydocket compatibility CI workflows.

Purpose: Verify burner-redis works across all supported Python versions (abi3-py310 means the .so is compatible, but we need to verify the Python-side code and test suite work on each version).
Output: Updated ci.yml and pydocket-compat.yml with matrix strategies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.github/workflows/ci.yml
@.github/workflows/pydocket-compat.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Python version matrix to CI test job</name>
  <files>.github/workflows/ci.yml</files>
  <action>
In `.github/workflows/ci.yml`, modify ONLY the `test` job (NOT the `integration-test` job):

1. Add a `strategy` block at the top of the `test` job (after `runs-on`):
```yaml
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12", "3.13", "3.14"]
```

2. Update the "Set up Python" step:
   - Change the step name from `Set up Python 3.12` to `Set up Python ${{ matrix.python-version }}`
   - Change `python-version: "3.12"` to `python-version: ${{ matrix.python-version }}`

3. Leave the `integration-test` job completely unchanged (it stays on Python 3.12).
4. Leave the `build` and `sdist` jobs completely unchanged.
  </action>
  <verify>
    <automated>cd /Users/alexander/dev/prefectlabs/burner-redis && grep -A3 'strategy:' .github/workflows/ci.yml | head -20 && echo "---" && grep 'matrix.python-version' .github/workflows/ci.yml && echo "---" && grep -A2 'integration-test:' .github/workflows/ci.yml</automated>
  </verify>
  <done>The test job has a 5-version Python matrix with fail-fast: false. The integration-test job is unchanged on Python 3.12.</done>
</task>

<task type="auto">
  <name>Task 2: Add Python version matrix to pydocket compatibility job</name>
  <files>.github/workflows/pydocket-compat.yml</files>
  <action>
In `.github/workflows/pydocket-compat.yml`, modify the `pydocket-compat` job:

1. Add a `strategy` block at the top of the job (after `runs-on`):
```yaml
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12", "3.13", "3.14"]
```

2. Update the "Set up Python" step:
   - Change the step name from `Set up Python 3.12` to `Set up Python ${{ matrix.python-version }}`
   - Change `python-version: "3.12"` to `python-version: ${{ matrix.python-version }}`

3. Leave everything else in the workflow unchanged.
  </action>
  <verify>
    <automated>cd /Users/alexander/dev/prefectlabs/burner-redis && grep -A3 'strategy:' .github/workflows/pydocket-compat.yml && echo "---" && grep 'matrix.python-version' .github/workflows/pydocket-compat.yml</automated>
  </verify>
  <done>The pydocket-compat job has a 5-version Python matrix with fail-fast: false.</done>
</task>

</tasks>

<verification>
Both workflow files have Python version matrices covering 3.10-3.14 with fail-fast: false. The integration-test job in ci.yml remains unmodified on Python 3.12.
</verification>

<success_criteria>
- ci.yml test job uses matrix strategy with python-version: ["3.10", "3.11", "3.12", "3.13", "3.14"]
- ci.yml integration-test job unchanged (still hardcodes Python 3.12)
- pydocket-compat.yml uses same matrix strategy
- Both jobs use fail-fast: false
- setup-python steps reference ${{ matrix.python-version }}
</success_criteria>

<output>
After completion, create `.planning/quick/260415-us1-add-python-version-matrix-to-pydocket-an/260415-us1-SUMMARY.md`
</output>
