---
phase: quick-260417-mj8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - Cargo.lock
  - .planning/quick/260417-mj8-cut-release-0-1-2-bump-cargo-toml-cargo-/RELEASE_NOTES.md
autonomous: true
requirements:
  - RELEASE-0.1.2
must_haves:
  truths:
    - "Cargo.toml declares version = \"0.1.2\" for the burner-redis package"
    - "Cargo.lock records burner-redis 0.1.2 in the [[package]] entry"
    - "RELEASE_NOTES.md exists in the quick dir with a proposed release name + 2 alternates and noir-voiced body"
    - "No tags are pushed, no GitHub releases are created, no branches are pushed during execution"
    - "The release-workflow tag vs manifest guard would pass for tag v0.1.2 against the updated Cargo.toml/Cargo.lock"
  artifacts:
    - path: "Cargo.toml"
      provides: "Package version bumped to 0.1.2"
      contains: "version = \"0.1.2\""
    - path: "Cargo.lock"
      provides: "Lockfile entry for burner-redis bumped to 0.1.2"
      contains: "name = \"burner-redis\"\nversion = \"0.1.2\""
    - path: ".planning/quick/260417-mj8-cut-release-0-1-2-bump-cargo-toml-cargo-/RELEASE_NOTES.md"
      provides: "Drafted release notes + proposed name for v0.1.2"
      min_lines: 40
  key_links:
    - from: "Cargo.toml [package].version"
      to: "Cargo.lock [[package]] burner-redis version"
      via: "version string must match exactly"
      pattern: "0\\.1\\.2"
    - from: ".github/workflows/release.yml verify-version job"
      to: "Cargo.toml + Cargo.lock on tag v0.1.2"
      via: "Would-pass check (not executed here, but invariant preserved)"
      pattern: "version = \"0.1.2\""
---

<objective>
Cut the 0.1.2 release locally: bump `Cargo.toml` and `Cargo.lock` from 0.1.1 → 0.1.2, draft the release notes in the quick-task directory, and propose a release name that continues the noir/crime-scene voice established by v0.1.0 ("Strike a match") and v0.1.1 ("Tying up loose ends").

**What is explicitly NOT done here (user handles):** pushing tags, pushing branches, creating the GitHub release, uploading artifacts, or publishing to PyPI. This plan leaves the repo in a state where the user can review the diff + notes, then take the release action themselves.

Purpose: Prepare a reviewable release bundle (manifest bump + release notes + name proposal) without any hard-to-reverse actions.

Output:
- Modified `Cargo.toml` (version 0.1.1 → 0.1.2)
- Modified `Cargo.lock` (burner-redis entry 0.1.1 → 0.1.2)
- New `.planning/quick/260417-mj8-cut-release-0-1-2-bump-cargo-toml-cargo-/RELEASE_NOTES.md` containing the proposed release name (plus 2 alternates) and a noir-voiced release body describing the 0.1.2 fixes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@Cargo.toml
@Cargo.lock
@pyproject.toml

# Prior release voice (for tone calibration). DO NOT re-fetch these in
# every task; the executor should read `gh release view v0.1.1` once
# at the top of Task 2 to lock the voice, then draft from memory.
#
# v0.1.0 — "Strike a match" — introduction / arson metaphor.
# v0.1.1 — "Tying up loose ends" — cleanup, witnesses silenced, paper trail.
# v0.1.2 — this release — hardening the shutdown path so Windows stops leaving fingerprints.
</context>

<interfaces>
<!-- Exact text the executor must touch. No hunting required. -->

From Cargo.toml (line 3):
```toml
[package]
name = "burner-redis"
version = "0.1.1"   # → bump to "0.1.2"
edition = "2024"
```

From Cargo.lock (lines 36-52, the burner-redis [[package]] block):
```toml
[[package]]
name = "burner-redis"
version = "0.1.1"   # → bump to "0.1.2"
dependencies = [
 "bytes",
 "mlua",
 "ordered-float",
 "parking_lot",
 "pyo3",
 "pyo3-async-runtimes",
 "rmp-serde",
 "serde",
 "sha1",
 "thiserror",
 "tokio",
]
```

From pyproject.toml (lines 5-10): version is `dynamic` — driven by maturin from `Cargo.toml`. DO NOT edit pyproject.toml.

Commits landed since v0.1.1 (from `git log v0.1.1..HEAD --oneline`):
```
2d4128b fix: stabilize Windows Docket CI by hardening pubsub teardown and delivery (#1)
9c8eca5 docs(explore): capture maturin tag-versioning research + follow-up todos/seed
d1e3e56 docs(debug): archive resolved release-gate investigation
```
Only `2d4128b` is user-visible. The two `docs(*)` commits stay out of the release notes (noise).

Release-worthy content of PR #1 (verbatim from the planning context):
1. Pubsub listener shutdown — three-pronged stop (AtomicBool `stop_flag` + oneshot `stop_tx` + JoinHandle) so `PubSub.aclose()` guarantees no `call_soon_threadsafe` fires after loop teardown.
2. Graceful client shutdown — `Store.shutdown` AtomicBool; `BurnerRedis.aclose()/close()` wakes blocking `xread`/`xreadgroup` waiters and stops all pubsub listeners; blocking loops check `is_shutdown()` and return empty.
3. `__aenter__`/`__aexit__` on `BurnerRedis` — async context manager support matching `redis.asyncio.Redis`.
4. Pubsub delivery decoupled from the asyncio loop (follow-up fix in the same PR).
5. Rust unit tests for `Store` shutdown semantics.
6. CI: new docket Windows test suite workflow (windows-latest, Py 3.10–3.14, working-tree build).
7. Release workflow: tag ↔ `Cargo.toml`/`Cargo.lock` version guard (prevents the v0.1.1-style tag-without-manifest-bump mishap).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Bump Cargo.toml and Cargo.lock to 0.1.2</name>
  <files>Cargo.toml, Cargo.lock</files>
  <action>
Bump both manifests from 0.1.1 → 0.1.2. Order and mechanics matter so the release-workflow tag/manifest guard would pass.

Step 1 — Edit `Cargo.toml`:
  - Change line 3 from `version = "0.1.1"` to `version = "0.1.2"`.
  - Leave every other line untouched (dependencies, features, [lib], [package] name, edition).

Step 2 — Update `Cargo.lock`. Prefer the lockfile-only cargo update so Cargo re-checksums correctly:

    cargo update -p burner-redis

  - Do NOT run `cargo update` without a package filter (that would churn unrelated dependency versions and balloon the diff).
  - If `cargo` is unavailable in the executor's environment, fall back to editing `Cargo.lock` manually:
      * Locate the `[[package]]` block where `name = "burner-redis"` (around line 36 — line numbers may shift, but it is the only block with that name).
      * Change its `version = "0.1.1"` line to `version = "0.1.2"`.
      * Do NOT touch any other [[package]] block, any `checksum = "…"` line, or the top-level `version = 4` at line 3 of Cargo.lock (that is the lockfile format version, NOT the package version).

Step 3 — Verify the diff is minimal:
  - `git diff Cargo.toml` should show exactly one changed line: `version = "0.1.1"` → `version = "0.1.2"`.
  - `git diff Cargo.lock` should show exactly one changed `version = "…"` line inside the `burner-redis` [[package]] block. Under `cargo update -p burner-redis`, the top-of-file `# @generated` comment will be preserved and no other package versions should move.

Why not edit `pyproject.toml`: it declares `dynamic = ["version"]` — maturin derives the wheel version from `Cargo.toml` at build time. Touching pyproject.toml here would be a no-op at best and a source of drift at worst.
  </action>
  <verify>
    <automated>grep -E '^version = "0\.1\.2"$' Cargo.toml && awk '/^\[\[package\]\]$/{p=0} /^name = "burner-redis"$/{p=1} p && /^version = /{print; exit}' Cargo.lock | grep -F 'version = "0.1.2"' && test -z "$(git diff --name-only -- Cargo.toml Cargo.lock | grep -vE '^(Cargo\.toml|Cargo\.lock)$')"</automated>
  </verify>
  <done>
    - `Cargo.toml` line 3 reads `version = "0.1.2"`.
    - The `burner-redis` entry in `Cargo.lock` reads `version = "0.1.2"`.
    - `git diff` shows changes only to `Cargo.toml` and `Cargo.lock` (no other files touched).
    - No tag was created, no branch was pushed, no GitHub release was opened. (These are the user's steps, out of scope.)
  </done>
</task>

<task type="auto">
  <name>Task 2: Draft release notes and propose a release name</name>
  <files>.planning/quick/260417-mj8-cut-release-0-1-2-bump-cargo-toml-cargo-/RELEASE_NOTES.md</files>
  <action>
Write a new file at `.planning/quick/260417-mj8-cut-release-0-1-2-bump-cargo-toml-cargo-/RELEASE_NOTES.md` containing:

1. A **header block** listing the proposed release name + 2 alternates. The user will pick one. This header is metadata for the user — it will be stripped (or the chosen title lifted) when the release is actually published. Mark it clearly so it isn't pasted into the GitHub release body verbatim.

2. The **release body** — a noir-voiced changelog in the same register as v0.1.0 and v0.1.1. See tone rules below.

Before drafting the body, run once to re-calibrate the voice:

    gh release view v0.1.1

Lock the register from its opening lines ("The fire's going; now we clean up. `v0.1.0` struck the match. …"). Do NOT re-read v0.1.0 unless uncertain — v0.1.1 is the closest tonal neighbor.

**Tone rules (non-negotiable):**
- Noir / crime-scene / confidential-informant voice. Short sentences. Dry, understated.
- Lean on the running metaphor: v0.1.0 lit the match, v0.1.1 wiped prints, v0.1.2 shores up the exits — specifically on Windows, where the shutdown path was leaving fingerprints (call_soon_threadsafe firing after the loop was down, listeners outliving the client).
- Prefer the library's established vocabulary from the prior notes: "witness", "paper trail", "silenced", "the scene", "the boss", "amateur hour", "dead drop", "the story we've been telling", etc. Reuse, don't parrot.
- Do NOT add emojis. Do NOT write a "Test Plan" or "Checklist" section. Do NOT include the v0.1.1 changelog text — link to the compare view instead.

**Proposed release name (primary)** — lead with this, then list 2 alternates. Given 0.1.2 is about locking the back door (graceful shutdown, pubsub teardown, Windows CI):

- Primary: **"No loose threads"** — continues the cleanup arc; a "thread" pulls double duty (the pubsub Tokio task, the async task, the figurative loose end).
- Alternate A: **"Lock the back door"** — Windows-flavored, shutdown-flavored.
- Alternate B: **"Don't leave the loop running"** — direct callback to the asyncio loop teardown bug that made this release necessary.

The executor MAY swap in better names if they fit the voice better, but MUST propose exactly 3 options (1 primary + 2 alternates), and each option MUST be one short phrase in the noir register — no generic "Stability release" language.

**Required sections in the body** (match v0.1.1's structure):
- Opening paragraph — 2-3 sentences framing 0.1.2 against the arc of 0.1.0 → 0.1.1 → 0.1.2. Mention that Windows was the one jurisdiction still keeping notes on us.
- A section covering the pubsub + shutdown fixes. Call out specifically:
  * `PubSub.aclose()` now actually stops the listener (three-pronged stop — AtomicBool, oneshot, JoinHandle).
  * `BurnerRedis.aclose()` / `close()` wakes blocking `xread` / `xreadgroup` waiters and stops all pubsub listeners. Blocking loops check `is_shutdown()` and return empty.
  * `async with BurnerRedis(...) as r:` now works — `__aenter__` / `__aexit__` match `redis.asyncio.Redis`.
  * Pubsub delivery decoupled from the asyncio loop (a follow-up fix within the same PR).
- A "Paper trail" / "Covering the paper trail" style section covering:
  * New docket Windows test suite workflow (windows-latest, Py 3.10–3.14, working-tree build).
  * Release workflow gains a tag ↔ `Cargo.toml` / `Cargo.lock` version guard. Reference the v0.1.1-style mishap this prevents without naming it as a shame — understated noir, not apologetic.
  * Rust unit tests for `Store` shutdown semantics.
- An "Upgrading" section — one short paragraph, `pip install --upgrade burner-redis`, no breaking changes.
- Close with: `**Full Changelog**: https://github.com/prefectlabs/burner-redis/compare/v0.1.1...v0.1.2`

**Do NOT include** in the body:
- The two `docs(*)` commits (9c8eca5, d1e3e56) — internal planning, not user-visible.
- A bullet per internal PR review comment.
- Any "Test Plan" section (explicitly prohibited by user's global CLAUDE.md for PR descriptions; we apply the same restraint to release notes).

**File skeleton** (fill in — do not ship this literal template):

```markdown
# v0.1.2 — Proposed release names

> This header is for user review. When publishing, pick one title and use only the body below.

- **Primary:** 0.1.2 — <primary name>
- Alt A: 0.1.2 — <alt A>
- Alt B: 0.1.2 — <alt B>

---

<release body — noir voice — sections as specified above>

**Full Changelog**: https://github.com/prefectlabs/burner-redis/compare/v0.1.1...v0.1.2
```
  </action>
  <verify>
    <automated>test -f .planning/quick/260417-mj8-cut-release-0-1-2-bump-cargo-toml-cargo-/RELEASE_NOTES.md && test "$(wc -l < .planning/quick/260417-mj8-cut-release-0-1-2-bump-cargo-toml-cargo-/RELEASE_NOTES.md)" -ge 40 && grep -qF 'Primary:' .planning/quick/260417-mj8-cut-release-0-1-2-bump-cargo-toml-cargo-/RELEASE_NOTES.md && grep -qF 'compare/v0.1.1...v0.1.2' .planning/quick/260417-mj8-cut-release-0-1-2-bump-cargo-toml-cargo-/RELEASE_NOTES.md && grep -qiE 'aclose|shutdown|pubsub' .planning/quick/260417-mj8-cut-release-0-1-2-bump-cargo-toml-cargo-/RELEASE_NOTES.md && grep -qiE 'windows' .planning/quick/260417-mj8-cut-release-0-1-2-bump-cargo-toml-cargo-/RELEASE_NOTES.md && ! grep -qiE '^##+ *test plan' .planning/quick/260417-mj8-cut-release-0-1-2-bump-cargo-toml-cargo-/RELEASE_NOTES.md</automated>
  </verify>
  <done>
    - `RELEASE_NOTES.md` exists at the specified path.
    - Header lists exactly one primary + two alternate names, each a short noir-register phrase.
    - Body covers the pubsub/shutdown fixes, async context manager support, Windows docket workflow, release-workflow tag/manifest guard, and Rust shutdown unit tests.
    - Body ends with a link to the v0.1.1…v0.1.2 compare URL.
    - No "Test Plan" section, no emojis, no mention of the internal `docs(*)` commits.
    - File is ≥ 40 lines (not a stub).
  </done>
</task>

</tasks>

<verification>
After both tasks run, the executor should confirm — without taking any destructive or publish-shaped action:

1. `git status` shows exactly three paths dirty: `Cargo.toml`, `Cargo.lock`, and the new `RELEASE_NOTES.md` under the quick dir. Nothing else.
2. `git diff Cargo.toml Cargo.lock` shows only version-line bumps from 0.1.1 → 0.1.2.
3. `git tag --list 'v0.1.2'` is empty — we have not created the tag. The user does that.
4. `git log origin/main..HEAD` shows no new commits from this plan (commit is also the user's step, unless they instruct otherwise at the quick-task wrap-up).
5. The `RELEASE_NOTES.md` file opens with the proposed-names header and closes with the compare-URL line.

If any of those fail, stop and report — do NOT attempt to "fix forward" by creating a tag or pushing.
</verification>

<success_criteria>
- `Cargo.toml` version is `0.1.2`.
- `Cargo.lock` `burner-redis` package entry is `0.1.2`.
- `.planning/quick/260417-mj8-cut-release-0-1-2-bump-cargo-toml-cargo-/RELEASE_NOTES.md` exists, carries a noir-voiced body covering the 0.1.2 changes, and proposes 1 primary + 2 alternate release names.
- No tags, branches, or GitHub releases were created during execution.
- Diff is reviewable in one sitting: three files, minimal churn.
</success_criteria>

<output>
After completion, the executor writes a quick-task summary at:
`.planning/quick/260417-mj8-cut-release-0-1-2-bump-cargo-toml-cargo-/260417-mj8-SUMMARY.md`

The summary should state:
- Which primary release name the executor landed on (and the two alternates).
- Confirmation that only `Cargo.toml`, `Cargo.lock`, and the new `RELEASE_NOTES.md` were modified.
- Explicit reminder to the user of the remaining manual steps (commit the bump, push the tag `v0.1.2`, create the GitHub release with the chosen title + body from `RELEASE_NOTES.md`) — phrased as a checklist, not executed.
</output>
