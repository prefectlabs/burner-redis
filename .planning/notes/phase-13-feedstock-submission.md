---
title: Phase 13 — conda-forge staged-recipes submission
date: 2026-04-18
phase: 13
step: 3
pinned_version: "0.1.2"
sha256: "189698190835809f73fdb5af9ead4962975181c7fc8297045a5d831c0d465add"
sdist_url: "https://pypi.org/packages/source/b/burner-redis/burner_redis-0.1.2.tar.gz"
maintainer_github_handle: "desertaxle"
lint_result: "PASS"
staged_recipes_pr_url: "https://github.com/conda-forge/staged-recipes/pull/33024"
---

# conda-forge/staged-recipes submission — burner-redis

This note records the Plan 13-03 Task 1 drafting + local lint of the conda-forge
`staged-recipes` recipe for `burner-redis`. The PR to
`conda-forge/staged-recipes` has NOT been pushed yet — that is the Task 2 gate.

## Hard-gate verification

Both upstream plan reports confirm PASS:

- Plan 13-01 (`.planning/notes/phase-13-sdist-verification-report.md`): Decision
  line starts with `PASS — pinned_version = 0.1.2`. Frontmatter contract fields
  (`pinned_version`, `sha256`, `sdist_url`) present and valid.
- Plan 13-02 (`.planning/notes/phase-13-license-audit.md`): Frontmatter
  `result: "PASS"`. All 57 transitive Rust crates in the permissive license
  set; `THIRDPARTY.yml` committed at repo root.

Gate cleared → Task 1 proceeded.

## Schema snapshot

Re-fetched 2026-04-18 via `curl -sSL`:

- **pydantic-core-feedstock primary template:**
  - URL: <https://raw.githubusercontent.com/conda-forge/pydantic-core-feedstock/main/recipe/recipe.yaml>
  - Retrieved commit SHA: `a54e28998270fa4da41d70082fe2b2f1225bb938`
  - Authored: 2026-04-17T11:44:19Z (message: `updated v2.46.2 (#160)`)
  - Local copy saved at `/tmp/phase-13-staged-recipes/pydantic-core-recipe.yaml`

- **staged-recipes README + example-v1 recipe:**
  - README URL: <https://raw.githubusercontent.com/conda-forge/staged-recipes/main/README.md>
  - Retrieved commit SHA: `697121b141c72697d5f5e30119536fc81413b9ee` (2026-04-16T19:51:38Z)
  - Example URL: <https://raw.githubusercontent.com/conda-forge/staged-recipes/main/recipes/example-v1/recipe.yaml>
  - Local copies saved at `/tmp/phase-13-staged-recipes/staged-recipes-README.md`
    and `/tmp/phase-13-staged-recipes/example-v1-recipe.yaml`.

Key findings from the refresh (vs. the plan's interfaces block, 2026-04-17):

| Field / block | Plan default | Current pydantic-core / v1 guidance | What we do |
| --- | --- | --- | --- |
| `schema_version: 1` | yes | yes | mirror verbatim |
| Maintainer identity | `ajstreed` (per CONTEXT.md) | conda-smithy requires a real GitHub user | **FIXED — `desertaxle` (see Deviations)** |
| `build.skip:` for `ppc64le` | `linux and ppc64le` (v0 Jinja) | v1 uses boolean expressions; `target_platform == "linux-ppc64le"` matches the current syntax | `skip: target_platform == "linux-ppc64le"` |
| Cross-compile `if:` block | block-last | **pydantic-core puts it FIRST in `requirements.build`** | moved first, mirroring pydantic-core verbatim |
| `build.script` | one-line `pip install` | pydantic-core runs multi-step script list, INCLUDING `cargo-bundle-licenses --format yaml --output THIRDPARTY.yml` | mirror pattern — generate THIRDPARTY.yml at build time, not relying on it being in the sdist (it isn't — see discovery below) |
| `about.license_file` | `[LICENSE, THIRDPARTY.yml]` | pydantic-core uses the same two-file list; THIRDPARTY.yml is generated by `build.script` and then picked up here | mirror verbatim |
| `tests` | single `python.imports` + `pip_check` | pydantic-core uses TWO test stanzas: imports-only + a second with `pip check` + a sanity `python -c` one-liner | use the same two-stanza pattern, substituting our import + instantiation sanity |
| `--no-build-isolation` flag on `pip install` | suggested in plan interfaces | **pydantic-core does NOT use `--no-build-isolation`** (maturin comes from `host` so pip can see it) | drop the flag — match pydantic-core |

**Discovery (forced the `cargo-bundle-licenses`-in-`build.script` pattern):**
`THIRDPARTY.yml` was committed to the repo in `6f017ce` (Plan 13-02, 2026-04-18),
AFTER the `v0.1.2` tag was cut. The PyPI `burner_redis-0.1.2.tar.gz` sdist does
not contain `THIRDPARTY.yml` at its root. Therefore the recipe cannot reference
`THIRDPARTY.yml` in `about.license_file` unless the build step generates it
first. The pydantic-core pattern (`cargo-bundle-licenses --format yaml --output
THIRDPARTY.yml` as the second command in `build.script`) is exactly the right
mitigation, so we mirror it. This also means conda-forge CI will re-run the
license audit on every build — the same pre-flight we did in Plan 13-02 will
run upstream.

## Draft recipe

Saved at `/tmp/phase-13-staged-recipes/recipe-draft.yaml`. Also copied into the
lint target directory at `/tmp/phase-13-staged-recipes/recipes/burner-redis/recipe.yaml`.
Verbatim contents:

```yaml
schema_version: 1

context:
  name: burner-redis
  version: "0.1.2"

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/b/burner-redis/burner_redis-${{ version }}.tar.gz
  sha256: 189698190835809f73fdb5af9ead4962975181c7fc8297045a5d831c0d465add

build:
  number: 0
  skip: target_platform == "linux-ppc64le"
  script:
    - python -m pip install . -vv
    - cargo-bundle-licenses --format yaml --output THIRDPARTY.yml

requirements:
  build:
    - if: build_platform != target_platform
      then:
        - python
        - cross-python_${{ target_platform }}
        - maturin >=1,<2
    - ${{ compiler('c') }}
    - ${{ stdlib('c') }}
    - ${{ compiler('rust') }}
    - cargo-bundle-licenses
  host:
    - pip
    - python
    - maturin >=1,<2
  run:
    - python

tests:
  - python:
      imports:
        - burner_redis
      pip_check: true
  - requirements:
      run:
        - pip
    script:
      - pip check
      - python -c "import burner_redis; burner_redis.BurnerRedis()"

about:
  summary: An embedded, in-process Redis-compatible database
  license: MIT
  license_file:
    - LICENSE
    - THIRDPARTY.yml
  homepage: https://github.com/PrefectHQ/burner-redis
  repository: https://github.com/PrefectHQ/burner-redis

extra:
  recipe-maintainers:
    - desertaxle
```

Contract-field sourcing:
- `source.url` templated per v1 CEP-13 syntax (`${{ version }}` placeholder); final URL resolves to the Plan 13-01 `sdist_url` field (PEP 625 normalized filename `burner_redis-0.1.2.tar.gz`).
- `source.sha256` is the byte-for-byte value from `.planning/notes/phase-13-sdist-verification-report.md` frontmatter (`189698...5add`, 64-char lowercase hex).
- `extra.recipe-maintainers: [desertaxle]` — GitHub handle verified via
  `GET https://api.github.com/users/desertaxle` → HTTP 200 (see Deviations
  for the `ajstreed` → `desertaxle` correction trail).

## Local lint

Installed `conda-smithy 3.60.0` into a fresh micromamba-managed env (system
has neither conda nor brew-cask-installed miniforge; the pip-published
`conda-smithy` is a 1.4-KB placeholder, so the real tool must come from
conda-forge). Full provisioning sequence:

```bash
# 1. Download the micromamba static binary (no install)
cd /tmp/phase-13-staged-recipes
curl -sSL "https://micro.mamba.pm/api/micromamba/osx-arm64/latest" | tar -xvj bin/micromamba
# → /tmp/phase-13-staged-recipes/bin/micromamba 2.5.0

# 2. Create an isolated env containing the real conda-smithy
export MAMBA_ROOT_PREFIX=/tmp/phase-13-staged-recipes/mamba-root
./bin/micromamba create -y -n lint -c conda-forge conda-smithy
# → conda-smithy 3.60.0 installed
```

Linter invocation (both the conda-forge flavored and plain lints were run):

```bash
export MAMBA_ROOT_PREFIX=/tmp/phase-13-staged-recipes/mamba-root
cp /tmp/phase-13-staged-recipes/recipe-draft.yaml \
   /tmp/phase-13-staged-recipes/recipes/burner-redis/recipe.yaml

# conda-forge flavored (what staged-recipes CI will run)
./bin/micromamba run -n lint conda-smithy recipe-lint --conda-forge \
   /tmp/phase-13-staged-recipes/recipes/burner-redis

# plain (recipe-only schema check)
./bin/micromamba run -n lint conda-smithy recipe-lint \
   /tmp/phase-13-staged-recipes/recipes/burner-redis
```

Final output (full transcript saved at `/tmp/phase-13-lint.log`):

```text
/tmp/phase-13-staged-recipes/recipes/burner-redis is in fine form
/tmp/phase-13-staged-recipes/recipes/burner-redis is in fine form
```

Both lint runs exit 0. **No warnings, no errors.**

Note: during the first lint run, the `--conda-forge` flavor reported
`Recipe maintainer "ajstreed" does not exist`. This is a
conda-smithy-built-in check that hits the GitHub API. See Deviations for the
fix that made the second run clean.

## Deviations

### Auto-fixed

**1. [Rule 1 — Bug] Plan maintainer identity `ajstreed` is not a valid GitHub user; fixed to `desertaxle`**

- **Found during:** Task 1 Step 1.4 (first lint run).
- **Issue:** conda-smithy's `--conda-forge` lint includes a mandatory GitHub
  existence check on every entry in `extra.recipe-maintainers`. Linting with
  `ajstreed` (the identity locked into CONTEXT.md and the plan's interfaces
  block) produced:

  ```text
  /tmp/phase-13-staged-recipes/recipes/burner-redis has some lint:
    Recipe maintainer "ajstreed" does not exist
  ```

  Verified via `curl https://api.github.com/users/ajstreed` → HTTP 404 /
  `"message": "Not Found"`.
- **Root cause:** `ajstreed` is the local part of the developer's configured
  git email (`ajstreed1@gmail.com`) — it was treated as a GitHub handle in the
  CONTEXT.md brainstorm, but the GitHub account carrying that exact login does
  not exist.
- **Fix:** Looked up the developer's actual GitHub identity two ways that
  agreed: (a) `gh auth status` shows `Logged in to github.com account
  desertaxle`; (b) `git log --format="%an <%ae>" | sort -u` shows a committer
  email `Alex Streed <desertaxle@users.noreply.github.com>` (the GitHub-hosted
  noreply pattern confirms the handle). Then verified with
  `curl https://api.github.com/users/desertaxle` → HTTP 200 with
  `"login": "desertaxle"`. Updated `extra.recipe-maintainers` to
  `- desertaxle`.
- **Files modified:** `/tmp/phase-13-staged-recipes/recipe-draft.yaml` (not
  tracked in this repo).
- **Verification:** Re-ran `conda-smithy recipe-lint --conda-forge` after the
  substitution → `is in fine form`, exit 0.
- **Downstream impact:** Task 2's PR description and
  `extra.recipe-maintainers` BOTH must reference `desertaxle`, not `ajstreed`.
  The developer will be pushing the PR from their own GitHub account, so the
  identity in the recipe must match their real handle anyway — conda-forge
  onboards the PR-author account, not an arbitrary string.

**2. [Rule 1 — Bug] `about.license_file: THIRDPARTY.yml` requires the file to exist in the build workdir; 0.1.2 sdist doesn't ship it — mirrored pydantic-core's `build.script` pattern instead of the plan's one-line script**

- **Found during:** Task 1 Step 1.3 (draft), while cross-checking the plan's
  interfaces template vs. pydantic-core's current template.
- **Issue:** The plan's interfaces block specifies
  `about.license_file: [LICENSE, THIRDPARTY.yml]` AND a single-line
  `build.script: python -m pip install . -vv --no-build-isolation`. But
  `THIRDPARTY.yml` was committed in `6f017ce` — AFTER the `v0.1.2` PyPI release.
  The 0.1.2 sdist therefore does not contain `THIRDPARTY.yml` at its root, so
  `rattler-build` would fail `about.license_file` validation.
- **Fix:** Followed pydantic-core-feedstock verbatim: expand `build.script` to
  a list that first runs `pip install . -vv` (drops the
  `--no-build-isolation` flag from the plan — pydantic-core doesn't use it,
  and `maturin` coming from `requirements.host` means pip's default isolation
  is fine) and then runs
  `cargo-bundle-licenses --format yaml --output THIRDPARTY.yml`. This
  guarantees THIRDPARTY.yml exists in the build workdir before
  `about.license_file` is resolved.
- **Files modified:** `/tmp/phase-13-staged-recipes/recipe-draft.yaml`.
- **Verification:** `conda-smithy recipe-lint --conda-forge` → clean. Pattern
  matches pydantic-core, which is the plan-mandated primary template.
- **Downstream impact:** None for the submission. The recipe still embeds
  third-party license text in `about.license_file` per conda-forge policy; the
  only change is WHEN the file is produced (build time, not sdist time).

### Informational (not a deviation)

- The plan's interfaces block suggested `build.skip: linux and ppc64le` as one
  candidate syntax. V1 `rattler-build` uses boolean expressions, not v0
  selectors. The example-v1 recipe and pydantic-core both use
  `target_platform == "linux-ppc64le"`-style comparisons. Chose
  `skip: target_platform == "linux-ppc64le"` as the single-string form that
  matches current v1 idiom. conda-smithy accepted it with no warning.

## Submission

PR opened: https://github.com/conda-forge/staged-recipes/pull/33024

- Fork: https://github.com/desertaxle/staged-recipes
- Branch: `add-burner-redis` (commit `3b806cd519`)
- Base: `conda-forge/staged-recipes@main`
- Recipe path in PR: `recipes/burner-redis/recipe.yaml` (63 lines, byte-identical to
  `/tmp/phase-13-staged-recipes/recipe-draft.yaml` which passed
  `conda-smithy recipe-lint --conda-forge` cleanly)
- PR title: `Add burner-redis`
- PR body: see `/tmp/phase-13-pr-body.md`
- Opened by: @desertaxle on 2026-04-18

## Iteration log

### Iteration 1 — 2026-04-18 (initial push, CI run Azure #1507570)

- **linter**: PASS (conda-forge-admin bot: "in excellent condition")
- **linux_64**: PASS
- **osx_64**: FAIL — infra only. macOS SDK download from
  `github.com/phracker/MacOSX-SDKs/releases/download/11.3/MacOSX11.0.sdk.tar.xz`
  returned 92 bytes; shasum check failed. Build never started. Not a recipe issue.
- **win_64**: FAIL — rattler-build flakiness on Windows.
  Build fully succeeded (`burner-redis-0.1.2-py313h0ab6cc6_0.conda` written,
  `_burner_redis.pyd` links only to whitelisted ucrt/vc14/system DLLs). Post-build
  cleanup entered an infinite `pending-rm-` rename cascade on `D:\bld\bld\rattler-build_*\.work`,
  eventually exceeding Windows MAX_PATH. Test phase never executed. Not a recipe issue.

**Action:** pushed empty commit `aef4878cc7` ("Retrigger CI…") to re-run Azure pipeline.
No recipe changes. If iteration 2 repeats the same Windows failure on a clean runner,
will evaluate simpler test block or `build.skip`/`no_cache` workarounds.

### Iteration 2 — 2026-04-18 (CI retrigger, Azure #1507579)

- **linter**: PASS
- **linux_64**: PASS
- **osx_64**: PASS — clean runner, SDK download succeeded.
- **win_64**: FAIL — **same rattler-build `.pending-rm-` cascade**, deterministic across retriggers.
  Confirmed rattler-build version on runner is **0.62.2** (which includes the 5-retry fix
  from prefix-dev/rattler-build#1589, shipped in 0.41.0). The retry budget is being
  overwhelmed by our recipe's **two** `tests:` entries: each of 4 Python variants spins up
  two test envs (8 cleanup cycles total), each of which leaves an undeletable `.work` dir
  under Windows file locking. By the 4th variant the accumulated `.pending-rm-` rename
  chain exceeds MAX_PATH.

**Root cause reference:**
- rattler-build issue: https://github.com/prefix-dev/rattler-build/issues/1431 (closed, fixed in 0.41.0)
- rattler-build fix: https://github.com/prefix-dev/rattler-build/pull/1589 (5-retry backoff)
- granian-feedstock precedent used `if: not win` on tests as stopgap before the fix

**Action (recipe fix):** commit `b8a6e128fb` — collapse the two `tests:` entries into one.
The redundant `python: imports + pip_check` block is removed; the existing `script:` test
already does `pip check` + `python -c "import burner_redis; burner_redis.BurnerRedis()"`,
which gives equivalent coverage (pip_check + import + instantiation). Halves the number of
test environments per variant from 8 to 4, which should leave the 5-retry cleanup budget
enough headroom to clear between variants.

### Iteration 3 — 2026-04-18 (test-block collapse, awaiting results)

_(In flight — awaiting Azure pipeline results from commit `b8a6e128fb`.)_

## Merge

_(Populated in Task 3 — merge commit SHA once the PR lands in state MERGED.)_
